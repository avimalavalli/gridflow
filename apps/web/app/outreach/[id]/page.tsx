/* eslint-disable @typescript-eslint/no-explicit-any */
import Link from "next/link";
import { notFound } from "next/navigation";
import { ArrowLeft, Building2, ContactRound, Linkedin, Mail } from "lucide-react";
import { Shell } from "../../../components/shell";
import { StatusBadge } from "../../../components/status-badge";
import { EmptyState } from "../../../components/empty-state";
import { apiGet, ApiError } from "../../../lib/server-api";
import { OutreachEditor } from "./outreach-editor";
export const dynamic="force-dynamic";
type Item=Record<string,any>;interface Detail{outreach:Item;versions:Item[];approvals:Item[];interactions:Item[];evidence:Item[];policy:Item;workflow:Item;channelActions:Item[]}
const dt=(v:string)=>new Intl.DateTimeFormat("en-GB",{day:"numeric",month:"short",year:"numeric",hour:"2-digit",minute:"2-digit"}).format(new Date(v));
export default async function OutreachDetailPage({params}:{params:Promise<{id:string}>}){const{id}=await params;let d:Detail;try{d=await apiGet<Detail>(`/outreach/${id}`)}catch(e){if(e instanceof ApiError&&e.status===404)notFound();throw e}const o=d.outreach;return <Shell title="Outreach Review"><div className="detail-hero"><div className="detail-identity"><span className="detail-logo">E</span><div><Link className="table-sub" href="/outreach"><ArrowLeft size={12}/> Back to outreach</Link><h1>{o.contactName}</h1><div className="detail-meta"><Link href={`/companies/${o.companyId}`}>{o.companyName}</Link><span>{o.jobTitle}</span><StatusBadge value={o.approvalStatus}/><StatusBadge value={o.linkedinStatus}/><StatusBadge value={o.emailStatus}/></div></div></div><div className="detail-actions"><Link className="button button-secondary" href={`/companies/${o.companyId}`}><Building2 size={14}/>Company</Link><Link className="button button-secondary" href={`/contacts/${o.contactId}`}><ContactRound size={14}/>Contact</Link>{o.linkedinProfileUrl?<a className="button button-secondary" href={o.linkedinProfileUrl} target="_blank" rel="noreferrer"><Linkedin size={14}/>LinkedIn</a>:null}{o.email?<a className="button button-secondary" href={`mailto:${o.email}`}><Mail size={14}/>Email</a>:null}</div></div><div className="split-layout"><div><OutreachEditor outreach={o as any} workflow={d.workflow as any} policy={d.policy as any}/></div><aside className="stack"><section className="card soft"><div className="section-header"><div><div className="eyebrow">Why this message</div><h2>Personalisation evidence</h2></div></div><div className="rich-copy">{o.personalisationEvidence||"No personalisation evidence was stored."}</div><div className="section-gap"><div className="eyebrow">Partnership angle</div><div className="rich-copy" style={{marginTop:8}}>{o.partnershipPitch||o.partnershipAngle||"No pitch recorded."}</div></div></section><section className="card"><div className="section-header"><div><div className="eyebrow">Approval history</div><h2>Human decisions</h2></div><span className="badge neutral">{d.approvals.length}</span></div>{d.approvals.length===0?<EmptyState title="No review decision" copy="Approve, reject or request changes from the workbench."/>:<div className="timeline">{d.approvals.map(a=><div className="timeline-item" key={a.id}><span className="timeline-dot"/><div><div className="timeline-title">{a.userName} · {a.decision.replaceAll("_"," ")}</div><div className="timeline-copy">{a.comments}</div><div className="timeline-time">{dt(a.createdAt)}</div></div></div>)}</div>}</section><section className="card"><div className="section-header"><div><div className="eyebrow">Version history</div><h2>Immutable drafts</h2></div><span className="badge neutral">{d.versions.length}</span></div><div className="queue">{d.versions.map(v=><div className="queue-item" key={v.id}><div className="queue-main"><div className="queue-title">Version {v.versionNumber}</div><div className="queue-copy">{v.modelUsed} · {v.promptVersion} · {dt(v.generatedAt)}</div></div>{v.id===o.currentVersionId?<StatusBadge value="ACTIVE_CONVERSATION"/>:<StatusBadge value="COMPLETED"/>}</div>)}</div></section><section className="card"><div className="section-header"><div><div className="eyebrow">Evidence</div><h2>Supporting sources</h2></div></div>{d.evidence.length===0?<EmptyState title="No sources attached" copy="Echo evidence references will appear here."/>:<div className="evidence-list">{d.evidence.map(e=><div className="evidence-item" key={e.id}><a href={e.url} target="_blank" rel="noreferrer">{e.title||e.publisher||e.url}</a><p>{[e.publisher,e.claimKey,e.retrievedAt?dt(e.retrievedAt):null].filter(Boolean).join(" · ")}</p></div>)}</div>}</section></aside></div></Shell>}
