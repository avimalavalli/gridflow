"use client";
import{useState}from"react";import{useRouter}from"next/navigation";import{Check,Copy,ExternalLink,Pause,Send,ThumbsDown}from"lucide-react";
type Outreach={id:string;linkedinConnectionNote:string|null;linkedinFollowUpMessage:string|null;emailSubject:string|null;emailBody:string|null;followUpEmail1:string|null;followUpEmail2:string|null;callOpener:string;partnershipPitch:string;generationNotes:string|null;linkedinProfileUrl:string|null;email:string|null;approvalStatus:string;linkedinStatus:string};
export function OutreachEditor({outreach}:{outreach:Outreach}){const router=useRouter();const[form,setForm]=useState({linkedinConnectionNote:outreach.linkedinConnectionNote??"",linkedinFollowUpMessage:outreach.linkedinFollowUpMessage??"",emailSubject:outreach.emailSubject??"",emailBody:outreach.emailBody??"",followUpEmail1:outreach.followUpEmail1??"",followUpEmail2:outreach.followUpEmail2??"",callOpener:outreach.callOpener??"",partnershipPitch:outreach.partnershipPitch??"",generationNotes:outreach.generationNotes??""});const[busy,setBusy]=useState(false);const[msg,setMsg]=useState("");async function request(path:string,body:unknown){setBusy(true);setMsg("");try{const r=await fetch(`/backend/outreach/${outreach.id}/${path}`,{method:path==="version"?"PATCH":"POST",credentials:"include",headers:{"content-type":"application/json"},body:JSON.stringify(body)});if(!r.ok){const b=await r.json()as{message?:string|string[]};throw new Error(Array.isArray(b.message)?b.message.join(" "):b.message||"Action failed.")}setMsg("Saved.");router.refresh()}catch(e){setMsg(e instanceof Error?e.message:"Action failed.")}finally{setBusy(false)}}async function copy(text:string,label:string){await navigator.clipboard.writeText(text);setMsg(`${label} copied.`)}return <div className="stack"><section className="card"><div className="section-header"><div><div className="eyebrow">Message editor</div><h2>LinkedIn</h2></div><div className="channel-actions">{outreach.linkedinProfileUrl?<a className="button button-secondary" href={outreach.linkedinProfileUrl} target="_blank" rel="noreferrer"><ExternalLink size={14}/>Open profile</a>:null}<button className="button button-secondary" disabled={!form.linkedinConnectionNote} onClick={()=>copy(form.linkedinConnectionNote,"Connection note")}><Copy size={14}/>Copy note</button></div></div><div className="message-editor"><div className="field"><label>Connection note</label><textarea value={form.linkedinConnectionNote} onChange={e=>setForm({...form,linkedinConnectionNote:e.target.value})}/><small>{form.linkedinConnectionNote.length} characters</small></div><div className="field"><label>Follow-up message</label><textarea value={form.linkedinFollowUpMessage} onChange={e=>setForm({...form,linkedinFollowUpMessage:e.target.value})}/></div></div></section><section className="card"><div className="section-header"><div><div className="eyebrow">Message editor</div><h2>Email</h2></div><button className="button button-secondary" disabled={!form.emailBody} onClick={()=>copy(`${form.emailSubject}\n\n${form.emailBody}`,"Email")}><Copy size={14}/>Copy email</button></div><div className="message-editor"><div className="field"><label>Subject</label><input value={form.emailSubject} onChange={e=>setForm({...form,emailSubject:e.target.value})}/></div><div className="field"><label>Email body</label><textarea value={form.emailBody} onChange={e=>setForm({...form,emailBody:e.target.value})}/></div><div className="grid-2 balanced"><div className="field"><label>Follow-up 1</label><textarea value={form.followUpEmail1} onChange={e=>setForm({...form,followUpEmail1:e.target.value})}/></div><div className="field"><label>Follow-up 2</label><textarea value={form.followUpEmail2} onChange={e=>setForm({...form,followUpEmail2:e.target.value})}/></div></div></div></section><section className="card"><div className="section-header"><div><div className="eyebrow">Commercial framing</div><h2>Pitch and call opener</h2></div></div><div className="form-grid"><div className="field"><label>Partnership pitch</label><textarea value={form.partnershipPitch} onChange={e=>setForm({...form,partnershipPitch:e.target.value})}/></div><div className="field"><label>Call opener</label><textarea value={form.callOpener} onChange={e=>setForm({...form,callOpener:e.target.value})}/></div><div className="field full"><label>Internal generation notes</label><textarea value={form.generationNotes} onChange={e=>setForm({...form,generationNotes:e.target.value})}/></div></div></section><div className="card"><div className="section-header"><div><div className="eyebrow">Review control</div><h2>Save and approve</h2></div></div><div className="channel-actions"><button className="button button-secondary" disabled={busy} onClick={()=>request("version",form)}>Save edits</button><button className="button button-primary" disabled={busy} onClick={()=>request("decision",{decision:"APPROVED",comments:"Approved in outreach workbench."})}><Check size={14}/>Approve</button><button className="button button-danger" disabled={busy} onClick={()=>request("decision",{decision:"NEEDS_CHANGES",comments:"Needs revision in outreach workbench."})}><ThumbsDown size={14}/>Needs changes</button></div>{msg?<div className="notice section-gap">{msg}</div>:null}</div><section className="card"><div className="section-header"><div><div className="eyebrow">Manual LinkedIn control</div><h2>Record channel action</h2></div></div><div className="channel-actions"><button className="button button-primary" disabled={busy||outreach.approvalStatus!=="APPROVED"} onClick={()=>request("linkedin-action",{action:"CONNECTION_SENT",nextFollowUpAt:new Date(Date.now()+5*86400000).toISOString()})}><Send size={14}/>Connection sent</button><button className="button button-secondary" disabled={busy} onClick={()=>request("linkedin-action",{action:"ACCEPTED",nextFollowUpAt:new Date(Date.now()+86400000).toISOString()})}><Check size={14}/>Accepted</button><button className="button button-secondary" disabled={busy} onClick={()=>request("linkedin-action",{action:"FOLLOW_UP_SENT",nextFollowUpAt:new Date(Date.now()+5*86400000).toISOString()})}>Follow-up sent</button><button className="button button-secondary" disabled={busy} onClick={()=>request("linkedin-action",{action:"REPLIED"})}>Reply received</button><button className="button button-danger" disabled={busy} onClick={()=>request("linkedin-action",{action:"PAUSED",notes:"Paused manually."})}><Pause size={14}/>Pause</button></div></section></div>}
