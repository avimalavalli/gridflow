"use client";
import Link from "next/link";
import { useMemo,useState } from "react";
import { ArrowUpRight, Linkedin, Mail, Search } from "lucide-react";
import { EmptyState } from "../../components/empty-state";
import { RunAgentButton } from "../../components/run-agent-button";
import { StatusBadge } from "../../components/status-badge";
export interface ContactListItem{id:string;contactName:string;jobTitle:string;companyName:string;companyId:string;email:string|null;linkedinProfileUrl:string|null;department:string;status:string;contactPriority:string;preferredChannel:string;echoStatus:string;companyPriority:string|null;confidence:number|null;nextFollowUpAt:string|null;outreachCount:number}
export function ContactsTable({contacts}:{contacts:ContactListItem[]}){const[q,setQ]=useState("");const[priority,setPriority]=useState("ALL");const[status,setStatus]=useState("ALL");const filtered=useMemo(()=>contacts.filter(c=>{const term=q.toLowerCase();return(!term||[c.contactName,c.jobTitle,c.companyName,c.department].some(v=>v.toLowerCase().includes(term)))&&(priority==="ALL"||c.contactPriority===priority)&&(status==="ALL"||c.status===status)}),[contacts,q,priority,status]);return <section className="card flush"><div className="toolbar" style={{padding:"16px 16px 0"}}><div className="toolbar-group"><div className="search-input"><Search size={14}/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search people, roles or companies"/></div><select className="filter-select" value={priority} onChange={e=>setPriority(e.target.value)}><option value="ALL">All priorities</option><option>PRIMARY</option><option>SECONDARY</option><option>BACKUP</option></select><select className="filter-select" value={status} onChange={e=>setStatus(e.target.value)}><option value="ALL">All statuses</option>{["NOT_CONTACTED","CONTACTED","REPLIED","MEETING_SCHEDULED","ACTIVE_CONVERSATION","UNRESPONSIVE"].map(v=><option key={v}>{v}</option>)}</select></div><span className="badge neutral">{filtered.length} shown</span></div>{filtered.length===0?<EmptyState title="No matching contacts" copy="Change the filters or run Relay on a qualified company."/>:<div className="table-wrap"><table><thead><tr><th>Contact</th><th>Company</th><th>Priority</th><th>Status</th><th>Channels</th><th>Echo</th><th>Outreach</th><th></th></tr></thead><tbody>{filtered.map(c=>{const channel=Boolean(c.email||c.linkedinProfileUrl);const eligible=["NOT_STARTED","FAILED","DRAFT_READY"].includes(c.echoStatus)&&channel&&["PRIMARY","SECONDARY"].includes(c.contactPriority)&&["HIGH","MEDIUM"].includes(c.companyPriority??"");return <tr key={c.id}><td><Link className="table-link" href={`/contacts/${c.id}`}><div className="table-primary">{c.contactName}</div><div className="table-sub">{c.jobTitle} · {c.department.replaceAll("_"," ")}</div></Link></td><td><Link className="table-link" href={`/companies/${c.companyId}`}><div className="table-primary">{c.companyName}</div><div className="table-sub">{c.companyPriority||"Unscored"} company</div></Link></td><td><StatusBadge value={c.contactPriority}/></td><td><StatusBadge value={c.status}/></td><td><div className="row-actions" style={{justifyContent:"flex-start"}}>{c.email?<a title={c.email} href={`mailto:${c.email}`}><Mail size={14}/></a>:null}{c.linkedinProfileUrl?<a href={c.linkedinProfileUrl} target="_blank" rel="noreferrer"><Linkedin size={14}/></a>:null}<span className="table-sub">{c.preferredChannel.replaceAll("_"," ")}</span></div></td><td>{eligible?<RunAgentButton agentName="ECHO" contactId={c.id} label={c.echoStatus==="DRAFT_READY"?"Regenerate":"Run Echo"} forceRegenerate={c.echoStatus==="DRAFT_READY"}/>:<StatusBadge value={c.echoStatus}/>}</td><td><strong>{c.outreachCount}</strong></td><td><Link className="icon-button" href={`/contacts/${c.id}`}><ArrowUpRight size={14}/></Link></td></tr>})}</tbody></table></div>}</section>}
