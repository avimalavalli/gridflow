"use client";
import{useState}from"react";import{useRouter}from"next/navigation";
export function ContactEditor({contact}:{contact:{id:string;status:string;contactPriority:string;preferredChannel:string;notes:string|null;nextFollowUpAt:string|null;email:string|null;phone:string|null}}){const router=useRouter();const[form,setForm]=useState({status:contact.status,contactPriority:contact.contactPriority,preferredChannel:contact.preferredChannel,notes:contact.notes??"",nextFollowUpAt:contact.nextFollowUpAt?new Date(contact.nextFollowUpAt).toISOString().slice(0,16):"",email:contact.email??"",phone:contact.phone??""});const[busy,setBusy]=useState(false);const[msg,setMsg]=useState("");async function save(e:React.FormEvent){e.preventDefault();setBusy(true);setMsg("");try{const r=await fetch(`/backend/contacts/${contact.id}`,{method:"PATCH",credentials:"include",headers:{"content-type":"application/json"},body:JSON.stringify({...form,nextFollowUpAt:form.nextFollowUpAt?new Date(form.nextFollowUpAt).toISOString():null})});if(!r.ok){const b=await r.json()as{message?:string|string[]};throw new Error(Array.isArray(b.message)?b.message.join(" "):b.message||"Update failed.")}setMsg("Contact updated.");router.refresh()}catch(e){setMsg(e instanceof Error?e.message:"Update failed.")}finally{setBusy(false)}}return <form className="stack" onSubmit={save}><div className="field"><label>Relationship status</label><select value={form.status} onChange={e=>setForm({...form,status:e.target.value})}>{["NOT_CONTACTED","CONTACTED","REPLIED","MEETING_SCHEDULED","ACTIVE_CONVERSATION","UNRESPONSIVE"].map(v=><option key={v}>{v}</option>)}</select></div><div className="field"><label>Contact priority</label><select value={form.contactPriority} onChange={e=>setForm({...form,contactPriority:e.target.value})}><option>PRIMARY</option><option>SECONDARY</option><option>BACKUP</option></select></div><div className="field"><label>Preferred channel</label><select value={form.preferredChannel} onChange={e=>setForm({...form,preferredChannel:e.target.value})}>{["EMAIL","LINKEDIN","PHONE","EMAIL_AND_LINKEDIN","UNKNOWN"].map(v=><option key={v}>{v}</option>)}</select></div><div className="field"><label>Email</label><input type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></div><div className="field"><label>Phone</label><input value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/></div><div className="field"><label>Next follow-up</label><input type="datetime-local" value={form.nextFollowUpAt} onChange={e=>setForm({...form,nextFollowUpAt:e.target.value})}/></div><div className="field"><label>Notes</label><textarea value={form.notes} onChange={e=>setForm({...form,notes:e.target.value})}/></div><button className="button button-primary" disabled={busy}>{busy?"Saving…":"Save contact"}</button>{msg?<div className="notice">{msg}</div>:null}</form>}
